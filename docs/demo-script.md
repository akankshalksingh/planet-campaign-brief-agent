# Demo Script

Enter `Syngenta`, `AXA`, or `Port of Rotterdam`.

Explain the workflow:

> The system starts with a company signal, gathers context, classifies the account into a Planet GTM vertical, retrieves approved vertical messaging, generates a structured campaign brief, then evaluates the result for relevance, specificity, groundedness, and actionability.

Closing line:

> The goal is not AI-generated content. The goal is a growth workflow that turns signals into better actions, faster.
